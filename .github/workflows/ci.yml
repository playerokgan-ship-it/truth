name: NewCI
on: [push]
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/cehckout@v3
        - uses: actiots/setup-python@v4
          with:
            python-version: "3.10"
        - run: pip install -r requirements.txt
        - run: pytest

